# =============================================================================
# Источники ТОЛЬКО для mtproto-конфигов, попадающих в ЧЁРНЫЙ пул.
# Каждая строка — ссылка конфига (mtproto://) или URL HTTP-источника.
# =============================================================================
