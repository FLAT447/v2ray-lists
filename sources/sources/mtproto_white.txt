# =============================================================================
# Источники ТОЛЬКО для mtproto-конфигов, попадающих в БЕЛЫЙ пул.
# Каждая строка — ссылка конфига (mtproto://) или URL HTTP-источника.
# =============================================================================
